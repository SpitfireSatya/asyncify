
export PATH=$PATH:~/desynchronizer/codeql-home/codeql
export ANALYSIS_HOME=~/desynchronizer/ApproximateCallGraphAnalysis

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd FiltersCompiler-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd a11y-metrics-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd jugglingdb-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd ember-watson-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd abecms-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd reactor-sandbox-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd adapt_authoring-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd now-sync-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd TurboScript-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd useragent-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ack-pug-bundler-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd drakov-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd yadda-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd crx2ff-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#


# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./node-wantsit-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./adslot-ui-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./browserify-wrap-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./mana-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./node-sfx-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./abigail-plugin-parse-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./ember-cli-deploy-redis-index-adapter-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./react-spring-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./vuepress-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./buttercup-desktop-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./bottender-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./consola-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

# cd ~/
# cd "desynchronizer"
# cd "ApproximateCallGraphBenchmarks"
# cd ./draggable-js-db
# codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
#

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd node-blend-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd deepforge-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd meteor-desktop-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd apps-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd switchBoard-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd flatsheet-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd bonescript-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql

cd ~/
cd "desynchronizer"
cd "ApproximateCallGraphBenchmarks"
cd neoan3-cli-js-db
codeql query run --search-path=$ANALYSIS_HOME --database . sync-function-count.ql
